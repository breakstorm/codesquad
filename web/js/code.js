#!/usr/bin/python
-*- coding: utf-8 -*-

import os
print "달을 입력하세요(1-12):",
month = raw_input()
print "년도를 입력하세요(1-2020):",
year = raw_input()
os.system("cal " + month + " " + year)





// whether or not there is pre defined "variable"
wewer = "ok";
a = wewer;

//command + k : monitor clear
//command + / - :
//command () : original size
//text code should be written
//text coverage : gradle
//console.log() 에서 console은 객체, log는 함수, ()는 Syntax

//namespace 바탕화면에 폴더 안에 묶어 놓는지 파일 다 펼쳐놓는지 ㅋㅋ 그런것들을 묶어놓는 이름을 namespace. 자바스크립트에서 namespace는 객체
//괜히 빈 객체 만들고 {} 필요한것을 넣어놓으면 된다. .으로 접근해서 쓸 수있다. 이런식으로 쓰는 표현법을 namespace 라고 한다. 충돌을 방지하기 위해서
//객체를 활용하는 방법 중 하나이다.
//javascript는 타입을 선언하는 부분이 없다. 동적으로 실행될 때 타입이 결정된다.

//interpret language는 런타임에 타입이 결정되기 때문에 브레이크 포인트가 많이 필요


//function aaa() {

//}
//var aaa = function() {

//}
